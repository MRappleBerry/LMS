{
  "buildCommand": "cd lms-app/client && npm install && npm run build",
  "outputDirectory": "lms-app/client/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
